import { TelNumberPipePipe } from './tel-number-pipe.pipe';

describe('TelNumberPipePipe', () => {
  it('create an instance', () => {
    const pipe = new TelNumberPipePipe();
    expect(pipe).toBeTruthy();
  });
});
